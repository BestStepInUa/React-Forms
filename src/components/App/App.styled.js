import styled from 'styled-components';

const AppContainer = styled.div`
        margin-left: 10px;
        align-items: center;
        font-size: 18px;
        color: #010101;
      `

export default AppContainer